#!/usr/bin/env python3
"""Launcher, so the tool can be run straight out of a checkout."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from psuremote.cli import main

raise SystemExit(main())
